// This component shows a form with inputs for title and description
// On submit, it calls a function passed via props to send POST /api/tasks
// It takes user input and sends it to the backend to create a task
