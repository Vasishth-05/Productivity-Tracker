// This component shows a single task (title, status, etc.)
// It receives a task object as a prop
// It has two buttons: one to mark as completed, another to delete the task
// On clicking the complete button, it calls PUT /api/tasks/:id
// On clicking delete, it calls DELETE /api/tasks/:id
